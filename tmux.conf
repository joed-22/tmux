


# -- Basic Settings ---------------------------------------------

# Change prefix
unbind `
unbind C-a
set -g prefix `
set -g prefix2 C-a
bind ` send-prefix

set -g base-index 1
set -g pane-base-index 1

set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",xterm-256color:RGB"

set -g escape-time 300
set -g focus-events off

set -g mouse on

# set -g repeat-time 1000

set -g history-limit 10000

set -g renumber-windows on


set -g display-panes-time 10000









# -- Key bindings ---------------------------------------------
setw -g mode-keys vi
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

bind -n M-h resize-pane -L 2
bind -n M-j resize-pane -D 2
bind -n M-k resize-pane -U 2
bind -n M-l resize-pane -R 2

# unbind '"'
# unbind %

bind c new-window -c "#{pane_current_path}"
bind '|' split-window -h -c "#{pane_current_path}"
bind '\' split-window -h -c "#{pane_current_path}"
bind 'v' split-window -h -c "#{pane_current_path}"
bind '-' split-window -v -c "#{pane_current_path}"
bind '_' split-window -v -c "#{pane_current_path}"

unbind r
bind r source-file ~/.config/tmux/tmux.conf

bind-key f popup -E

bind b break-pane -d

bind j join-pane -h -s !

# -- Popup Windows ---------------------------------------------




# -- Status Bar & Styling ---------------------------------------------
# set -g status-position top
#
# set -g status-style bg=default,fg=black,bright
# set -g status-left ""
# set -g status-right "#[fg=black,bright]#S"
#
# set -g window-status-bell-style "bg=red,nobold"
# set -g window-status-current-style \
#     "#{?window_zoomed_flag,bg=yellow,bg=brightblue,nobold}"
#
# set -g window-status-separator ""
#
# set -g window-status-format "| #I: #[fg=brightblack]#W "
# set -g window-status-current-format "[ #[fg=black,bg=brightblue]#W ]"

# Catppuccin styling
set -g @plugin 'catppuccin/tmux'
# set -g @catppuccin_flavor "mocha"  # latte, frappe, macchiato or mocha
# set -g @catppuccin_window_status_style "basic"
# set -g @catppuccin_status_background "#262626"    # "default"
# set -g @catppuccin_status_left_separator "█"
# set -g @catppuccin_status_right_separator "█"

# Configure Catppuccin
set -g @catppuccin_flavor "mocha"
set -g @catppuccin_status_background "none"
set -g @catppuccin_window_status_style "none"
set -g @catppuccin_pane_status_enabled "off"
set -g @catppuccin_pane_border_status "off"

# Load catppuccin
run ~/.config/tmux/plugins/tmux/catppuccin.tmux


set -gu window-status-format
set -gu window-status-current-format
set -gu window-status-style
set -gu window-status-current-style
set -gu window-status-last-style
set -gu window-status-activity-style


# # Make the status line pretty and add some modules
# set -g status-right-length 100
# set -g status-left-length 100
# set -g status-left ""
# set -g status-right "#{E:@catppuccin_status_application}"
# set -ag status-right "#{E:@catppuccin_status_host}"
# set -agF status-right "#{E:@catppuccin_status_cpu}"
# set -agF status-right "#{E:@catppuccin_status_date_time}"


set -g @thm_bg "#131517" 


# status left look and feel
set -g status-left-length 100
set -g status-left ""
set -ga status-left "#{?client_prefix,#{#[bg=#{@thm_red},fg=#{@thm_bg},bold]  #S },#{#[bg=#{@thm_bg},fg=#{@thm_green}]  #S }}"
set -ga status-left "#[bg=#{@thm_bg},fg=#{@thm_overlay_0},none]│"
set -ga status-left "#[bg=#{@thm_bg},fg=#{@thm_maroon}]  #{pane_current_command} "
set -ga status-left "#[bg=#{@thm_bg},fg=#{@thm_overlay_0},none]│"
set -ga status-left "#[bg=#{@thm_bg},fg=#{@thm_blue}]  #{=/-32/...:#{s|$USER|~|:#{b:pane_current_path}}} "
set -ga status-left "#[bg=#{@thm_bg},fg=#{@thm_overlay_0},none]#{?window_zoomed_flag,│,}"
set -ga status-left "#[bg=#{@thm_bg},fg=#{@thm_yellow}]#{?window_zoomed_flag,  zoom ,}"

# status right look and feel
set -g status-right-length 100
set -g status-right ""
set -ga status-right "#[bg=#{@thm_bg},fg=#{@thm_green}]  #H "
set -ga status-right "#[bg=#{@thm_bg},fg=#{@thm_overlay_0},none]│"
set -ga status-right "#[bg=#{@thm_bg},fg=#{@thm_maroon}] 󰍛 #{cpu_percentage} "
set -ga status-right "#[bg=#{@thm_bg},fg=#{@thm_overlay_0},none]│"
set -ga status-right "#[bg=#{@thm_bg},fg=#{@thm_blue}] 󰭦 %m-%d-%Y 󰅐 %H:%M "




set -g status-position bottom
set -g status-style "bg=#{@thm_bg}"
set -g status-justify "absolute-centre"



# window look and feel
set -g allow-rename on
set -g automatic-rename on
# set -g automatic-rename-format '#{?pane_title,#{pane_title},#{pane_current_command}}'
set -g automatic-rename-format "#{pane_current_command}"
# set -g automatic-rename-format \
# '#{?pane_title,#{pane_title},#{?#{in:#{pane_current_command},bash,zsh,fish},#{b:pane_current_path},#{pane_current_command}}}'



# set -g window-status-format \
# "#[bg=#{@thm_overlay_0},fg=#{@thm_bg},bold] #I #[bg=#{@thm_bg},fg=#{@thm_rosewater}] #W "
# set -g window-status-style "bg=#{@thm_bg},fg=#{@thm_rosewater}"
# set -g window-status-last-style "bg=#{@thm_bg},fg=#{@thm_rosewater}"
# set -g window-status-activity-style "bg=#{@thm_red},fg=#{@thm_bg}"
# set -g window-status-bell-style "bg=#{@thm_red},fg=#{@thm_bg},bold"
# set -gF window-status-separator " "
#
# set -g window-status-current-format \
# "#[bg=#{@thm_blue},fg=#{@thm_bg},bold] #I #[bg=#{@thm_bg},fg=#{@thm_blue},bold] #W "
# set -g window-status-current-style "bg=#{@thm_blue},fg=#{@thm_bg},bold"
#



set -g window-status-format "#[bg=default,fg=#{@thm_overlay_0}] #I: #W #[bg=default,fg=#{@thm_overlay_0}]"
set -g window-status-activity-style "bg=#{@thm_red},fg=#{@thm_bg}"
set -g window-status-bell-style "bg=#{@thm_red},fg=#{@thm_bg},bold"
set -gF window-status-separator "#[bg=default,fg=#{@thm_rosewater}]│#[default]"

set -g window-status-current-format "#[bg=#{@thm_bg},fg=#{@thm_rosewater},bold] #I: #W #[default]"
set -g window-status-current-style "bg=#{@thm_bg},fg=#{@thm_rosewater},bold"



set -g status-interval 1








# -- TMP Plugins ---------------------------------------------

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'

# Other examples:
# set -g @plugin 'github_username/plugin_name'
# set -g @plugin 'github_username/plugin_name#branch'
# set -g @plugin 'git@github.com:user/plugin'
# set -g @plugin 'git@bitbucket.com:user/plugin'


set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @vim_navigator_mapping_left "C-h C-Left"
set -g @vim_navigator_mapping_down "C-j C-Down"
set -g @vim_navigator_mapping_up "C-k C-Up"
set -g @vim_navigator_mapping_right "C-l C-Right"



set -g @plugin 'tmux-plugins/tmux-resurrect'

set -g @plugin 'tmux-plugins/tmux-yank'

set -g @plugin 'tmux-plugins/tmux-cpu'


# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.config/tmux/plugins/tpm/tpm'

